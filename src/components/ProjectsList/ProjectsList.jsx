import ProjectDeleteButton from "../ProjectDeleteButton/ProjectDeleteButton.jsx";
import s from "./ProjectsList.module.css";
const STATUS={draft:"Rascunho",building:"Em construção",ready:"Prévia pronta",sent:"Enviado",approved:"Aprovado",published:"Publicado"};const TEMPLATE={institutional:"Site institucional",landing:"Landing page premium",menu:"Cardápio / delivery",booking:"Serviços / agendamento"};const DELETABLE=new Set(["draft","building"]);
export default function ProjectsList({projects=[]}){return <main className={s.page}><header className={s.header}><div><h1>Meus projetos</h1><p>Sites criados pelo builder do LeadFlow. Revise a prévia, refine por prompt e baixe o projeto completo em ZIP.</p></div><a href="/criar-site">+ Criar projeto</a></header>{projects.length===0?<section className={s.empty}><span>◇</span><h2>Nenhum projeto criado</h2><p>Abra um lead no CRM ou comece pelo criador de sites.</p><a href="/criar-site">Criar primeiro projeto</a></section>:<section className={s.grid}>{projects.map(project=><article className={s.card} key={project.id}><div className={s.cardTop}><span>{project.name.slice(0,1).toUpperCase()}</span><div><h2>{project.name}</h2><p>{[project.segment,project.city].filter(Boolean).join(" · ")||"Negócio sem categoria"}</p></div><b>{STATUS[project.status]||project.status}</b></div><dl><div><dt>Modelo</dt><dd>{TEMPLATE[project.template]||project.template}</dd></div><div><dt>Versão</dt><dd>{project.version||1}</dd></div><div><dt>Conteúdo</dt><dd>{project.aiUsed?"IA + direção premium":"Direção visual do nicho"}</dd></div><div><dt>Imagens</dt><dd>{project.imageCount||0} no projeto</dd></div><div><dt>Atualizado</dt><dd>{new Date(project.updatedAt).toLocaleString("pt-BR")}</dd></div></dl>{project.warning&&<div className={s.warning}>{project.warning}</div>}<div className={s.actions}><div className={s.navigationActions}>{project.leadId&&<a href={"/crm/"+project.leadId}>Abrir lead</a>}<a href={"/criar-site?project="+project.id+(project.leadId?"&lead="+project.leadId:"")}>Editar site</a><a href={"/api/projects/"+project.id+"/zip"}>Baixar ZIP</a></div>{DELETABLE.has(project.status)&&<ProjectDeleteButton projectId={project.id} projectName={project.name}/>}</div></article>)}</section>}</main>}
